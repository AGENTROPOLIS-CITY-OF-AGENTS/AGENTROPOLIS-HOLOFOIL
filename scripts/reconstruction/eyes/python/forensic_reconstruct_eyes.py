import sys

print("DISABLED: forensic_reconstruct_eyes.py destroyed valid trait color/opacity and produced low-fidelity fragments.")
print("Use source_exact_isolate_eyes.py for source-preserving individual trait isolation.")
print("Holofoil must never call these outputs mint masters. Mint promotion requires reconstruction + fidelity QC + human approval.")
sys.exit(2)
