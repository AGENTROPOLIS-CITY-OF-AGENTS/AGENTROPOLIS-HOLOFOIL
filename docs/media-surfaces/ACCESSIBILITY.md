# Accessibility

Public media needs title, alt text, and captions or a transcript when speech matters.

Keyboard: Enter/Space authorize or expand, Escape closes cinema, M mutes.

`reportAccessibility(records)` lists gaps. Missing title/alt blocks release.
