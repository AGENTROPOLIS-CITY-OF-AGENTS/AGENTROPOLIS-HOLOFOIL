# Theme schema

`HolofoilMediaTheme` (`theme-default` ships with the engine):

frameColor, frameMaterial, emissiveColor, glowStrength, screenBrightness, borderWidth, supportColor, loadingColor, errorColor, focusColor, typeColor.

Register overrides with `registerHolofoilTheme`. Do not hard-code brand colors in adapters.
