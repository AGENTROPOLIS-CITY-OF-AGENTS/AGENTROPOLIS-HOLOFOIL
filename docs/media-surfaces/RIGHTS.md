# Rights and provenance

Before public render Holofoil checks owner, license, `approved`, moderation, audience, rating, and schedule.

Fail closed on unapproved, expired, restricted, or missing rights. Do not fabricate ownership. Destinations are never inferred from filenames — set `destinationUrl` explicitly.
